[Service]
RuntimeDirectory=fprint
Environment=FP_VIRTUAL_IMAGE=/run/fprint/virtimg_sock
# log the matcher's score for every swipe:  journalctl -u fprintd | grep score   (>= 40 = match)
Environment=G_MESSAGES_DEBUG=libfprint-print
